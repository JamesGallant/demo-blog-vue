# CI/CD to run tests on commit
on:
 push:
  branches:
   - main
   - dev
 pull_request:
  branches:
   - main
   - dev

jobs:
 tests:
  name: "unit tests"
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v2
   - run: npm ci
   - npm: test
